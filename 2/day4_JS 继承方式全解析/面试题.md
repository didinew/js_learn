Q1：组合继承为什么父类调用了两次？

因为 Child.prototype = new Parent() 调用一次父类构造函数，Parent.call(this) 又调用一次父类构造函数。

Q2：ES6 class 是如何实现继承的？

ES6 class 内部依旧是原型链 + 构造函数组合继承的封装，super() 调用父类构造函数，方法挂载在父类 prototype 上。

Q3：为什么构造函数继承不能复用方法？

## 因为方法在构造函数里定义，每次实例化都会创建新函数，不共享原型链。
