Q1：proto 与 prototype 的区别？

__proto__ 在实例上，指向构造函数的 prototype；
prototype 在构造函数上，用于定义实例共享方法。

Q2：constructor 作用？

指向创建该 prototype 的构造函数，用于类型判断或实例还原。

Q3：原型链查找顺序？

先查实例 → 再查 proto → 上一级原型 → … → Object.prototype → null。

