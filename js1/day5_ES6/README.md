📘 Day 5｜ES6+ 新特性 · 解构 / 模板字符串 / 默认参数 / 扩展运算符

🎯 今日目标

- 熟练使用 解构赋值
- 掌握 模板字符串 写动态内容
- 熟练 函数默认参数
- 会用 扩展运算符 做数组/对象拷贝与合并
- 能在真实业务中灵活使用

## ⏱ 建议用时：60~120 分钟

📚 今日学习内容

1️⃣ 解构赋值

// 对象解构

```js
const user = { name: "Tom", age: 18 };
const { name, age } = user;
```

// 数组解构

```js
const arr = [1, 2, 3];
const [first, second] = arr;
```

✅ 面试常问点：

- 默认值
- 嵌套解构
- 交换变量值 [a, b] = [b, a]

---

2️⃣ 模板字符串

```js
const name = "Lucy";
const age = 22;

const str = `Hello ${name}, you are ${age} years old`;
```

✅ 注意：

- 可以写多行
- 可直接嵌入表达式 ${a + b}

---

3️⃣ 默认参数

```js
function greet(name = 'Guest') {
console.log(`Hello ${name}`)
```

}
greet() // Hello Guest

- 避免 undefined
- 支持复杂表达式作为默认值

---

4️⃣ 扩展运算符

// 数组合并

```js
const arr1 = [1, 2];
const arr2 = [3, 4];
const merged = [...arr1, ...arr2]; // [1,2,3,4]
```

// 对象合并 / 拷贝

```js
const obj1 = { a: 1 };
const obj2 = { b: 2 };
const copy = { ...obj1, ...obj2 }; // {a:1,b:2}
```

- 浅拷贝，不改变原对象/数组
- 可以做函数参数收集 function fn(...args) {}
