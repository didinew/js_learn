📘 Day 2｜数组进阶 · filter（筛选 & 搜索）

🎯 今日目标

- 掌握 filter 的使用场景
- 能写出 真实业务中的筛选逻辑
- 理解：filter = 条件判断 + 保留数据

## ⏱ 建议用时：60～90 分钟

📚 今日学习内容

1️⃣ filter 是干什么的？

一句话版本 👇

filter 用来“留下符合条件的元素”

```js
const arr = [1, 2, 3, 4];

const res = arr.filter((n) => n > 2);
```

// [3, 4]

filter 的 3 个关键点（必须记住）1. 返回新数组 2. 不修改原数组 3. 回调函数返回：

- true 👉 保留
- false 👉 丢弃

2️⃣ map vs filter（面试必对比）

| 场景 | 用谁 |
| --- | --- |
| 改变数据结构 | map |
| 条件筛选 | filter |
| 两者结合 | 非常常见 |
| list | .filter(item => item.done) |
.map(item => item.title)
